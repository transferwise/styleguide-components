import angular from 'angular';
import template from './loader.demo.html';

export default angular
  .module('tw.styleguide.demo.loading.loader', [])
  .component('twLoaderDocs', {
    template
  }).name;
