import angular from 'angular';
import CompleteCard from './complete-card.component';

export default angular
  .module('tw.styleguide.forms.upload.complete', [])
  .component('twUploadComplete', CompleteCard)
  .name;
