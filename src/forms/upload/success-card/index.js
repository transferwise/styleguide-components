import angular from 'angular';
import SuccessCard from './success-card.component';

export default angular
  .module('tw.styleguide.forms.upload.success', [])
  .component('twUploadSuccess', SuccessCard)
  .name;
