import angular from 'angular';
import DefaultCard from './default-card.component';

export default angular
  .module('tw.styleguide.forms.upload.default', [])
  .component('twUploadDefault', DefaultCard)
  .name;
