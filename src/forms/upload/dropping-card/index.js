import angular from 'angular';
import DroppingCard from './dropping-card.component';

export default angular
  .module('tw.styleguide.forms.upload.dropping', [])
  .component('twUploadDropping', DroppingCard)
  .name;
