import angular from 'angular';
import CaptureCard from './capture-card.component';

export default angular
  .module('tw.styleguide.forms.upload.capture', [])
  .component('twUploadCapture', CaptureCard)
  .name;
