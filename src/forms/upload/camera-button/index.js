import angular from 'angular';
import CameraButton from './camera-button.component';

export default angular
  .module('tw.styleguide.forms.upload.camera-button', [])
  .component('twCameraButton', CameraButton)
  .name;
