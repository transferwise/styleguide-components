import angular from 'angular';
import template from './focusable.demo.html';

export default angular
  .module('tw.styleguide.demo.forms.checkbox', [])
  .component('twFocusableDocs', { template }).name;
