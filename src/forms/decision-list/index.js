import angular from 'angular';
import DecisionList from './decision-list.component.js';

export default angular
  .module('tw.styleguide.forms.decision-list', [])
  .component('twDecisionList', DecisionList).name;
