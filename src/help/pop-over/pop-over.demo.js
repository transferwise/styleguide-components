import angular from 'angular';
import template from './pop-over.demo.html';

export default angular
  .module('tw.styleguide.demo.help.pop-over', [])
  .component('twPopOverDocs', { template }).name;
