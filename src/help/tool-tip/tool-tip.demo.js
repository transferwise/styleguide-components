import angular from 'angular';
import template from './tool-tip.demo.html';

export default angular
  .module('tw.styleguide.demo.help.tool-tip', [])
  .component('twToolTipDocs', { template }).name;
